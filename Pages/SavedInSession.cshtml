@page
@model SavedInSessionModel
@{
    ViewData["Title"] = "Data in session";
}
    
   
@if (Model.Latalista != null)
    {
        <p>Dane w zapisane w sesji:</p>
        for(int i=0;i<Model.Latalista.Count();i++)
        {
            <p>@Model.Latalista[i].Imie, @Model.Latalista[i].Number - @Model.Latalista[i].Powiadomienie</p>

        }
    }